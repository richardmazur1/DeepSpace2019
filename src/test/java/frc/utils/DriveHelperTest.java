package frc.utils;

import edu.wpi.first.wpilibj.Timer;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

// TODO add more and better tests for this and other functions
public class DriveHelperTest {
//    @Test
//    public void arcadeDriveToDriveSignal() {
//        final double STRAIGHT_X = 1.0;
//        final double STRAIGHT_Z = 0.0;
//        final DriveSignal straightDriveSignal = new DriveSignal(1.0, 1.0);
//        assertEquals(straightDriveSignal, DriveHelper.arcadeToDriveSignal(STRAIGHT_X, STRAIGHT_Z));
//        assertEquals(straightDriveSignal, DriveHelper.arcadeToDriveSignal(STRAIGHT_X, STRAIGHT_Z, true));
//        assertEquals(straightDriveSignal, DriveHelper.arcadeToDriveSignal(STRAIGHT_X, STRAIGHT_Z, false));
//
//        final double REVERSE_X = -1.0;
//        final double REVERSE_Z = 0.0;
//        final DriveSignal reverseDriveSignal = new DriveSignal(-1.0, -1.0);
//        assertEquals(reverseDriveSignal, DriveHelper.arcadeToDriveSignal(REVERSE_X, REVERSE_Z));
//        assertEquals(reverseDriveSignal, DriveHelper.arcadeToDriveSignal(REVERSE_X, REVERSE_Z, true));
//        assertEquals(reverseDriveSignal, DriveHelper.arcadeToDriveSignal(REVERSE_X, REVERSE_Z, false));
//
//        final double SPIN_RIGHT_X = 0.0;
//        final double SPIN_RIGHT_Z = 1.0;
//        final DriveSignal spinRightDriveSignal = new DriveSignal(1.0, -1.0);
//        assertEquals(spinRightDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_RIGHT_X, SPIN_RIGHT_Z));
//        assertEquals(spinRightDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_RIGHT_X, SPIN_RIGHT_Z, true));
//        assertEquals(spinRightDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_RIGHT_X, SPIN_RIGHT_Z, false));
//
//        final double SPIN_LEFT_X = 0.0;
//        final double SPIN_LEFT_Z = -1.0;
//        final DriveSignal spinLeftDriveSignal = new DriveSignal(-1.0, 1.0);
//        assertEquals(spinLeftDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_LEFT_X, SPIN_LEFT_Z));
//        assertEquals(spinLeftDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_LEFT_X, SPIN_LEFT_Z, true));
//        assertEquals(spinLeftDriveSignal, DriveHelper.arcadeToDriveSignal(SPIN_LEFT_X, SPIN_LEFT_Z, false));
//
//        final double ZERO_X = 0.0;
//        final double ZERO_Z = 0.0;
//        final DriveSignal zeroDriveSignal = new DriveSignal(0.0, 0.0);
//        assertEquals(zeroDriveSignal, DriveHelper.arcadeToDriveSignal(ZERO_X, ZERO_Z));
//        assertEquals(zeroDriveSignal, DriveHelper.arcadeToDriveSignal(ZERO_X, ZERO_Z, true));
//        assertEquals(zeroDriveSignal, DriveHelper.arcadeToDriveSignal(ZERO_X, ZERO_Z, false));
//    }
//
//    @Test
//    public void timer(){
//        Timer timer = new Timer();
//        System.out.println(timer.get());
//    }
}